<UserControl x:Class="WorldMapZoneControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FeneMonManagerWPF"
             mc:Ignorable="d"
             >
    <Border BorderThickness="1" BorderBrush="Black" Background="{Binding AffinityBackgroundColor}">
        <!--<Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="{Binding AffinityColor}" Offset="0.574"/>
                <GradientStop Color="White" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>-->
        <DockPanel LastChildFill="True">

            <Label DockPanel.Dock="Top" Content="{Binding Affinity}" Foreground="{Binding AffinityForegroundColor}"></Label>
            <Button x:Name="btnViewGym">View Gym</Button>
        </DockPanel>
    </Border>

</UserControl>
